Directive ::= DirectiveItem+

DirectiveItem ::= GateDef | LinkDef

GateDef ::= "DEFINE" GateIdentifier "AS" GateTy

GateTy ::= "not"
	| "and"
	| "or"
	| "xor"
	| "nand"
	| "nor"

GateIdentifier ::= [a-z_]+

LinkDef ::= "LINK" GateIdentifier "->" "INPUT" InputIdentifier "OF" GateIdentifier 

InputIdentifier ::= [a-z]
